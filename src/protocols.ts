/**
 * Serializable data contracts only -- plain types/interfaces describing data this project
 * persists, receives, or shares (wire formats, storage schemas). No behavior, no methods.
 * Behavioral interfaces (services this project's internals depend on) belong in contracts.ts
 * instead. See CLAUDE.md's Architecture conventions.
 *
 * Empty until this project actually has a data shape to describe -- don't pre-populate this
 * with speculative types.
 */

export {};
